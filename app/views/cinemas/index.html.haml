%h2=t('.about_header')
%p=t('.about_paragraph')

- if @cinemas.length != 0
  .container
    - @cinemas.each do |cinema|
      - if @counter.modulo(2) == 0
        .row
          .col-xs-6
            %img{:class => "image_cinema_index",:src => "#{cinema.image}", :alt => "#{t('.image_missing')}"}
          .col-xs-6
            %p
              %b= t('.name')
            %p= cinema.name
            %br
            %p
              %b= t('.name')
            %p= cinema.name

      - else
        .row
          .col-xs-6
            %p
              %b= t('.name')
            %p= cinema.name
            %br
            %p
              %b= t('.address')
            %p= cinema.address
          .col-xs-6
            %img{:class => "image_cinema_index", :src => "#{cinema.image}", :alt => "#{t('.image_missing')}"}
      - @counter+=1
      %br
      %hr
      %br
